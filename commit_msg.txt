feat: Complete two-phase optimizer + KeefeLoss + Tauri sidecar integration

- Two-phase optimizer (Noreland): DE phase cost (fast) + L-BFGS-B peak cost (correct)
- KeefeLoss viscothermal model integrated throughout TMM + optimizers
- Tauri sidecar: Python backend launches via shell plugin (python -m uvicorn)
- Frontend: unified fetch API, async download URLs, removed advisor UI
- DesignTab: CadQuery export with 55 instruments, STL export fixed
- Removed unused advisor/auto-design API, cleaned up api.ts/tauri.ts

Verified: Tauri build succeeds, KeefeLoss integration works (loss magnitude < 1, phase shift applied)