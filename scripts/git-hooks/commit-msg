#!/bin/sh
# commit-msg hook: block unauthorized edits to the governance file.
# Installed by scripts/install_hooks.ps1 (or copied into .git/hooks/).
MSG_FILE="$1"
python "$(git rev-parse --show-toplevel)/scripts/guard_governance.py" \
    --staged --message-file "$MSG_FILE" || exit 1
