Laptop (Kalle): PR #68 update — CI status matches base, not a regression.

Verification: the 3 failing checks on PR #68 (dependency-locks, guard, test/ruff) **already failed identically on `10c7268`** — the exact commit that was merged as PR #66 (`gh api .../commits/10c72681/check-runs` shows same failures). So PR #68 adds zero new CI failures.

- dependency-locks: `pip-compile` crashes with `ImportError: stdlib_pkgs from pip._internal.utils.compat` — pip-tools/pip version incompat in the runner, environmental.
- guard: `check_local_dependencies.py --warn` lists 31 missing optional deps (botorch, build123d, cadquery, chess, dask, ...) — CI only installs `.[dev]`, extras not installed. Environmental.
- test/ruff: `ruff check .` on the whole repo fails with 2628 errors (baseline debt in `backend/*`, `woodwind_designer/*` — none in the new GUI files).

Additionally: I ran `ruff check` on all new PR #68 files (`scripts/gui_automation/*`, `tests/test_gui_automation.py`, `scripts/volunteer_benchmark.py`) and **fixed all 35 lint findings** (24 auto + 11 manual: PEP 604 unions, `check=False`, narrowed exceptions, `TypeError` for type checks). Commit `a6eb853` pushed; now 3 commits on the PR, all ruff-clean. Compliance watchdog: "no new violations vs baseline".

PR #68 still requests desktop review/merge (governance ASK-directive fix + Fusion GUI pipeline). OpenRouter free-vision fallback remains untested — `OPENROUTER_API_KEY` still not found on laptop env or .env.
