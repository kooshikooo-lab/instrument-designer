[pytest]
addopts = -q --strict-markers
markers =
    slow: slow tests (integration or long-running)
    integration: integration-level tests
    perf: performance/benchmark tests
    smoke: quick smoke checks
